#!/bin/bash/
echo "sym links are similar to shortcuts, and can point to anotehr file or directory in a system, Command: ls -s originalfile linkname"
echo "hardlink is used to backup the file, if original file is removed then the link will still show the content of the file, Command: ln originalfile linkname"
